# 作業部屋（2つのセッションを同時に動かす）

2026-08-16 設置。

## なぜ要るのか

2026-08-15、同じフォルダで2つのセッションを同時に動かしたところ、
片方のコミットがもう片方のブランチに載った。PRの中身が空だったことで
気づけたが、気づかなければ無関係なコードが混ざったままマージされていた。

原因は **1つのフォルダは、同時に1つのブランチしか開けない** こと。
2つのセッションが同じフォルダを取り合うと、後から切り替えたほうが勝つ。
そこで、セッションごとにフォルダを分ける。

## 部屋の置き場所と名前

| 部屋 | フォルダ | 使いかた |
|---|---|---|
| 1つ目 | `~/nomad-hopper` | もとからあるフォルダ。**場所も名前も変えない** |
| 2つ目 | `~/nomad-hopper-b` | 必要なときに作り、終わったら片づける |
| 3つ目 | `~/nomad-hopper-c` | 同上 |
| 4つ目 | `~/nomad-hopper-d` | 同上 |

命名規則は **`nomad-hopper-` のうしろに b / c / d** を付けるだけ。
ブランチ名はフォルダ名に入れない。部屋の中で開くブランチは入れ替わるため、
名前に入れると、名前と中身がすぐ食い違う。

履歴（コミット・ブランチ）は1つのまま共有される。増えるのはフォルダだけ。

## 使いかた

道具は `~/ai-office/heya.sh` にある。
nomad-hopper の中には置かない。中に置くと、その道具を持たないブランチに
切り替えた瞬間に道具ごと消えるため。

### いまの部屋を見る

**ターミナルに貼ってください:**

```bash
cd ~/ai-office && ./heya.sh
```

どのフォルダでどのブランチが開いているかが並ぶ。

### 2つ目の部屋を用意する

**ターミナルに貼ってください（`feat/なにか` の部分は好きなブランチ名に）:**

```bash
cd ~/ai-office && ./heya.sh --new feat/なにか
```

空いている部屋を1つ選び、そのブランチを開く。
指定した名前のブランチが無ければ、main から新しく作る。
最後に「この部屋を使うには」として `cd ... && claude` の1行が表示されるので、
**新しいターミナルの窓を開いて**、その1行を貼る。これで2つ目のセッションになる。

道具が自動でやること:
- 履歴を最新にする
- そのブランチが別の部屋で開いていたら、止める（取り合いを防ぐ）
- 許可の設定・試作画面の設定・`.env.local` を、もとの部屋から写す
- 部品（npm install）を入れる
- 新しいブランチの送信先が main にならないようにする

### 使い終わった部屋を片づける

**ターミナルに貼ってください:**

```bash
cd ~/ai-office && ./heya.sh --close b
```

`b` のところは片づけたい部屋の名前。フォルダが消えるだけで、
**ブランチと履歴は残る**。

保存していない変更や、GitHubへ送っていないコミットが残っていると、
道具が中身を並べたうえで **片づけを断る**。捨てないための仕組み。
その場合は先にコミットして送信するか、内容を見たうえでもう一度実行する。

### 道具そのものを検査する

**ターミナルに貼ってください:**

```bash
cd ~/ai-office && ./heya.sh --kensa
```

道具に不具合が入り込んでいないかを調べる。「検査: 異常なし」と出れば大丈夫。

## 2つの見張り

本番ブランチへの誤送信は、性質の違う2つで止めている。片方では穴が残る。

| 見張り | 置き場所 | 何を見るか | 弱いところ |
|---|---|---|---|
| guard.sh | `~/.claude/hooks/` | AIが打つコマンドの**文字列** | 行き先が設定に隠れていると見えない |
| pre-push | `<リポジトリ>/.git/hooks/` | 送信の**本当の行き先** | AI以外の操作も含め、送信の瞬間しか見ない |

`pre-push` は、ブランチ名を書かない送信（`git push` だけ）も止める。
文字列には行き先が現れないため、guard.sh では止められない形である。

作業部屋は `.git/hooks` を共有するので、**1つ置けば部屋a・b・c・dすべてに効く**
（2026-08-16 実測）。`heya.sh` は実行のたびに置き直すので、「置いてあるはず」に
頼らなくてよい。ターミナルから人が打った送信にも効く。

どちらもgit管理外に置いてある。ブランチを切り替えても消えないようにするため。
正本は `~/ai-office/pre-push`。

## 気をつけること

- **日本語を含むブランチ名は作れない**。道具が断る。
  その名前で部屋を作ると、その部屋で立ち上げたセッションが、起動したとき
  自分のブランチ名を「HEAD」と取り違えるため（2026-08-16 に2回とも再現）。
  git に聞き直せば正しい名前が返るので壊れてはいないが、
  「いまどのブランチにいるか」の取り違えは、この道具が防ごうとしている
  事故そのもの。はじめは警告だけにしたが、警告は人の注意力に頼るので、
  機械で断る側に変えた。
- **印の無いフォルダは片づけない**。道具が作った部屋には `.heya-shirushi`
  という印が入っている。印が無いフォルダは、よそで作られたものかもしれず、
  中に何が入っているか分からないので、道具は消さずに断る。
  2026-08-16、AIの検査スクリプトが「これは自分が作った部屋だ」と思い込んで
  ウエマツさんの部屋を消した。消す前に確かめる手だてが無かったことが原因。
- **同じブランチを2つの部屋で同時に開くことはできない**。git が断る。
  これは制限ではなく、2026-08-15 の事故を起こさないための性質。
- 部屋ごとに部品（node_modules）と試作画面の番号は別。
  試作画面は空いている番号を自動で選ぶ設定になっている。

## 道具を直す人へ

**変数は必ず `${...}` と波括弧で書くこと。**

macOS標準の bash 3.2 は、文字設定がUTF-8のとき

```
"「$BRANCH」"
```

の `$BRANCH` の直後にある `」` を変数名の一部として飲み込む。結果:

```
./heya.sh: line 98: BRANCH?: unbound variable
```

厄介なのは、**文字設定が未設定の環境では再現しない**こと。
AIの実行環境は未設定、ウエマツさんのターミナルはUTF-8。
そのため AI 側で試して通っても、ターミナルで落ちる。
`./heya.sh --kensa` は文字設定をUTF-8に固定して調べるので、これで防げる。

## 実測で確かめたこと（2026-08-16）

- 片方の部屋でブランチを切り替えても、もう片方は変わらない（両方向で確認）
- 同じブランチを2つの部屋で開こうとすると git が断る
- 追加した部屋でも、危険な操作の見張り（guard.sh）が効く。
  見張りは `~/.claude/hooks/guard.sh` にあり、フォルダに関係なく働く
- 見張りが誤って普通の操作を止めることはない（送信・コミット・ビルド・
  部品入れ・後片づけ・部屋の増減で確認）
- 部屋b・c・dの3つすべてで、見張りが効くこと／誤って止めないことを確認
- 用意した部屋で `claude` が実際に立ち上がり、そのフォルダを自分の居場所として
  認識することを確認
- 断るべき場面で断ること（満室、すでに開いているブランチ、
  保存していない変更やGitHubに送っていないコミットが残っている部屋の片づけ）

## 2026-08-16 の追加分（穴を4つ塞いだ）

- 部屋を作るとき送信先を設定しない（`--no-track`）。作った直後に読み直して確かめる
- `pre-push` を入れ、行き先を書かない送信も止める。
  見張りを外した対照実験で、外すと本当に main へ飛ぶことを確認した
- 日本語を含むブランチ名は、警告ではなく作成を断る
- 見張りの控えをリポジトリから外した。控えは `~/ai-office/hooks_backup/` の1つだけ。
  リポジトリ内に置くとブランチごとに古い控えが取り残され、検査が
  「本物と違います」と言い続けて、本当の異常が埋もれるため
- 部屋には印（`.heya-shirushi`）を置き、印の無いフォルダは片づけない
